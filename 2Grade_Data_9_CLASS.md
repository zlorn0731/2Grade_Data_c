# 📚 2학년 데이터구조 (Data Structures)
## 9장 : 트리

### 트리(TREE)
- 트리 : 계층적인 구조를 나타내는 자료구조
- 트리는 부모-자식 관계의 노드들로 이루어짐
- 응용분야 : 회사의 조직도, 컴퓨터의 폴더 구조, 인공지는 바둑 프로그램의 거대한 결정 트리

### 트리의 용어
- 노드(node) : 트리의 구성요소
- 루트(root) : 부모(parent)가 없는 노드, A
- 서브 트리(subtree) : 하나의 노드와 자손들로 이루어짐
- 단말 노드(terminal node) : 자식(child)이 없는 노드
  - leaf node
- 비단말 노드(nonterminal node) : 자식을 가지는 노드
```
                (A) - 루트 노드
         ↙      ↓       ↘ - 간선, 에지(edge)
        (B)     (C)       (D)
     ↙ ↓ ↘     ↓         ↙↘
  (E) (F) (G)   (H)      (I) (J)
   서브 트리    서브 트리    서브 트리

[트리의 용어 : 루트, 서브 트리]
```
- 자식, 부모 형제(sibling), 조상(ancestor), 자속(descendent) 노드
  - 인간과 동일
- 차수(degree) : 어떤 노드의 자식 노드의 수
- 트리의 차수 : 트리가 가지고 있는 노드의 차수 중에서 가장 큰 값
- 레벨(level) : 트리의 각 층의 번호
- 높이(height) : 트리의 최대 레벨, 3
- 포리스트(forest) : 여러 개의 트리의 집합
```
                                                  레벨
                (A) - 부모 노드 ------------------- 1
         ↙      ↓       ↘ 
   나 - (B)     (C)       (D) - 형제 노드  -------- 2
     ↙ ↓ ↘     ↓         ↙↘
  (E) (F) (G)   (H)      (I) (J) ---------------- 3
   자손 노드

[트리의 용어 : 레벨, 부모, 형제, 자손 노드]
```
- 예제 8.1
  - 루트 노드 : A
  - 부모 노드 :
  - 형제 노드 : C, B
  - B의 차수(degree) : 2
  - 높이(height) : 4
```
            (A)
           ↙   ↘
         (B)      (C)
        ↙ ↘      ↙↓↘
       (D) (E)    (F)(G)(H)
                         ↓
                        (I)
```

### 일반 트리의 표현
- 일반 트리 : 노드 구조를 이용하여 표현
  - 데이터 필드, 링크 필드
  - 링크 필드의 개수 : 노드의 차수
  ```
  [일반 트리의 구조, 링크 배열의 길이는?]

  [데이터 필드] [링크1][링크2]       [링크n]
  |    data    |     |     |  ...  |     |
                ↙      ↓              ↘
           자식노드1   자식노드2         자식노드n
  ```
  - 링크를 배열로 표시할 경우 : 노드마다 배열의 길이가 다름
  - 연결 리스트를 사용
    - 첫 번째 자식을 가리키는 링크(child)
    - 형제를 가리키는 링크(sibling)
    - 부모는 자식들을 순서대로 접근
    - 문제🚨 : 너무 복잡함

### 이진 트리(binary tree)
- 모든 노드가 2개의 서브 트리를 가지고 있는 트리
  - 서브 트리는 공집합일 수 있음
  ```
    [데이터 필드] | [링크 필드] 
  |     data     | 링크1 | 링크2 |
                    ↙       ↘
               왼쪽자식    오른쪽자식

  [이진 트리의 노드 구조]
  ```
- 이진 트리(binary tree)
  - 각 노드에는 최대 2개까지의 자식 노드가 존재
  - 모든 노드의 차수가 2 이하가 된다
    - 구현하기가 편리함
  - 자식들간(서브 트리)간의 순서가 존재(왼쪽, 오른쪽)
  ```
         (루트)
        ↙    ↘
    T left    T right

  [이진 트리의 정의]
  ```
  - 정의 : 이진 트리
    - 공집합이거나
    - 루트와 왼쪽 서브 트리, 오른쪽 서브 트리로 구성된 노드들의 유한 집합으로 정의된다
    - 이진 트리의 서브 트리들은 모두 이진 트리여야 한다

  - 정의에 따라 그림 8.8이 이진트리인지 살펴보기
    - (예시) SUB3, SUB2, SUB1
    ```
                 (A)
               ↙    ↘
              (B)      (C)
             ↙ ↘       ↘  ↘
          (D)  공집합    공집합 공집합
         ↙ ↘
     공집합 공집합
    ```
    ```
    [SUB1]
    
      (B)
     ↙
    (D)
    -------------   
    [SUB2]

    (C)
    -------------
    [SUB3]

    (D)
    ```
    - (예시) 수식을 표현하는 이진 트리
    ```
               (+)
             ↙   ↘
           (*)     (/)
          ↙ ↘    ↙ ↘
        (a)  (b) (c)   (d)

    (a) (a*b)+(c/d)
    ```
    ```
             (+)
            ↙  ↘
         (+)     (d)
        ↙ ↘
      (+)   (c)
     ↙ ↘
    (a)  (b)

    (b) ((a+b)+c)+d
    ```

### 이진 트리의 성질
- 노드의 개수가 n개이면 간선의 개수는 n-1개
```
             (+)
           ↙   ↘
         (*)     (/)
        ↙ ↘    ↙ ↘
      (a)  (b) (c)   (d)

노드의 개수 : 7
간선의 개수 : 6
```
- 높이가 h : 최소 h개 ~ 최대 2h-1개의 노드를 가짐
```
 ㅡㅡ
  |         (1)                  (+) ------------------- 2¹⁻¹ = 1
  |         /                  ↙    ↘
높이=3     (2)               (*)       (/) ------------- 2²⁻¹ = 2
  |        /                ↙ ↘      ↙ ↘
  |      (3)              (a)  (b)   (c)  (d) ---------- 2³⁻¹ = 4
 ㅡㅡ
최소 노드 개수 = 3          최소 노드 개수 = 2¹⁻¹ + 2²⁻¹ + 2³⁻¹ = 1 + 2+ 4 = 7

[같은 높이의 이진트리에서 노드의 최소 개수와 최대 개수]
```
- n개 노드의 이진 트리 높이 : [ㅣlog₂(n+1)] 이상이고, n이하
```
 ㅡㅡ           
  |              (1)
  |              /
  |            (2)
  |            /
  |          (3)                   (1)                 ㅡㅡ
  |          /                    ↙  ↘                 |
높이=7     (4)                  (2)     (3)           높이=3
  |        /                  ↙ ↘     ↙ ↘            |
  |      (5)                (4)  (5) (6)    (7)        ㅡㅡ
  |      /
  |    (6)    
  |    /
  |  (7)     
 ㅡㅡ    

[노드의 개수가 동일한 이진트리에서의 높이의 차이]
```

### 이진 트리의 분류
- 이진 트리 형태에 따른 분류
  - 포화 이진 트리, 완전 이진 트리, 기타 이진 트리
  ```
         (A)
        ↙  ↘
      (B)     (C)
     ↙ ↘    ↙ ↘
   (D) (E)  (F)  (G)
  
  (a) 포화 이진 트리
  ```
  ```
             (A)
            ↙  ↘
          (B)     (C)
         ↙ ↘    ↙ ↘
        (D) (E)  (F)  (G)
       ↙ ↘  ↘
     (H)  (I)  (J)

  (b) 완전 이진 트리
  ```
  ```
             (A)
            ↙  ↘
          (B)     (C)
         ↙       ↙ ↘
        (D)     (F)  (G)
          ↘  
           (I)

  (c) 기타 이진 트리
  ```
  ```
  트리
    ↳ 이진 트리
            ↳ 편향 이진 트리
            ↳ 완전 이진 트리
                         ↳ 포화 이진 트리
  ```

#### 포화 이진 트리(full binary tree)
- 트리의 각 레벨에 노드가 꽉 차있는 이진 트리
- 높이 k
  - 2k-1개의 노드를 가짐
  - 전체 노드의 개수 : 2k - 1
- 노드의 번호
  - 노드에 번호를 붙일 수 있음
  - 레벨 단위로 왼쪽에서 오른쪽으로 순서대로 번호를 붙임
  - 번호는 항상 일정
    - (예시) 루트 노드의 오른쪽 자신 노드 : 항상 3

#### 완전 이진 트리(complete binary tree)
- 높이가 h일 때 레벨 1부터 h-1까지는 노드가 모두 채워짐
- 마지막 레벨 h에서는 노드가 순서대로 채워짐
  - 중간에 빈 곳이 있으면 안됨
- 노드의 번호
  - 포화 이진트리와 동일
- 이진 트리의 예시
  - 10장 힙(heap)
 
### 이진 트리의 추상 자료형
- 데이터
  - 노드의 집합, 공집합이거나, 루트 노드와 왼쪽 서브 트리, 오른쪽 서브 트리로 구성됨
    모든 서브 트리도 이진 트리이어야 함
- 연산
  - init() : 이진 트리를 초기화
  - is_empty() : 이진 트리가 공백 상태인지 확인
  - creat_tree(e, left, right)
  - 이진 트리 left와 right를 받아 e를 루트로 하는 이진 트리를 생성
  - get_root() : 이진 트리의 루트 노드를 반환
  - get_count() : 이진 트리의 노드의 수를 반환
  - get_leaf_count() : 이진 트리의 단말 노드의 수를 반환
  - get_height() : 이진 트리의 높이를 반환

### 이진 트리의 표현 : 배열 표현법
- 포화 이진 트리나 완전 이진 트리에 가장 적합
- 일반 이진 트리도 표현 가능
- 가정
  - 이진 트리가 완전 이진 트리라고 가정, 높이에 따라 배열의 길이를 결정
  - 높이 k : 길이 2k-1인 배열이 필요
  - 각 노드에 번호를 붙여서 그 번호를 배열의 인덱스로 삼아 노드의 데이터를 배열에 저장

- (예시) 인덱스 0은 사용하지 않음
```
(a) 완전 이진 트리
        
       (A)1                  
      ↙ ↘                  
    (B)2  (C)3
   ↙ ↘    ↘ 
 (D)4  (E)5  (F)6

0 |   |
1 | A |
2 | B |
3 | C |
4 | D |
5 | E |
6 | F |
7 |   |
8 |   |
```
```
(b) 경사 이진 트리

      (A) 1
      / 
    (B) 2
    /
  (C) 4
  /
(D) 8

0 |   |
1 | A |
2 | B |
3 |   |
4 | C |
5 |   |
6 |   |
7 |   |
8 | D |
```
- 어떤 노드의 인덱스를 알면, 그 노드의 부모나 자식의 인덱스 계산 가능
  - 노드 i의 부모 노드 인덱스 : i/2
  - 노드 i의 왼쪽 자식 노드 인덱스 : 2i
  - 노드 i의 오른쪽 자식 노드 인덱스 : 2i + 1
  - 문제점 🚨
    - 간단하지만, 기억 공간 낭비, 배열의 크기에 따라 트리의 높이 제한
   
### 이진 트리의 표현 : 링크 표현법
- 부모 노드가 자식 노드를 가리키게 하는 방법
  - 포인터를 이용
  ```
      | 링크1 | 데이터 | 링크2 |
        ↙                ↘ 
  왼쪽 자식 노드       오른쪽 자식 노드

  [링크표현법에서 이진 트리의 노드와 구조]
  ```
```
         (A)
        ↙  ↘
      (B)     (C)
     ↙ ↘    ↙ 
   (D) (E)  (F)

              | | A | |
            ↙         ↘    ↱ NULL
       | | B | |     | | C | |
      ↙       ↘        ↘
 | | D | |   | | E | |   | | F | |
  ↳NULL ↳NULL ↳NULL ↳NULL ↳NULL ↳NULL

(a) 완전 이진 트리
```
```
      (A) 
      / 
    (B) 
    /
  (C) 
  /
(D

                 | | A | |
                ↙
           | | B | |
          ↙
     | | C | |
    ↙
| | D | |

(b) 경사 이진 트리
```
